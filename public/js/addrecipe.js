const addBtn = document.getElementById('addNewIngredient');

addIngredientBox(event) => {
    const ingredientBox = document.createElement("ingredientBox");
    ingredientBox.setAttribute("type", "text");
    const deleteBtn = document.createElement('deleteBtn');
    deleteBtn.setAttribute
    document.getElementById("ingredients").appendChild(ingredientBox);
}

deleteIngredientBox(event) => {
    
}